export const STORAGE_KEYS = {
  SETTINGS: "diffapp_settings"
};

export const DB_CONFIG = {
  NAME: "DiffAppDatabase",
  VERSION: 1
};

export const UI_CONSTANTS = {
  SCROLL_SYNC_TIMEOUT_MS: 50,
  MIN_FONT_SIZE: 10,
  MAX_FONT_SIZE: 24,
  DEFAULT_FONT_SIZE: 13
};